// Retonando ou definindo a posição de um elemento
// $('form, field:eq(0)').offset({
//     top: 5,
//     left:100
// });

// Retonando a  posção de um elemento se bsaeando no elemento pai
// $('form, field:eq(0)').position();

// Retornando ou definindo a posicao da barra de rolagem vertical
// $(document).scrollTop(400)

// Retornando ou definindo a posicao da barra de rolagem horizontal
// $(document).scrollLeft(400)